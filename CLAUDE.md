# CLAUDE.md — このリポジトリで作業するときのルール

まなびの輪 / AI HACK 2026 提出物。**このリポジトリは提出時に public になる。**

---

## 最初に読むもの

| ファイル | 中身 |
|---|---|
| `DESIGN.md` | 設計の正。迷ったらここが基準 |
| `DESIGN.md` §8 | **絶対に壊してはいけないルール** |
| `README.md` | 審査員向けの説明 |
| `docs/evidence/README.md` | 証拠の置き方 |

---

## 絶対ルール

- `.env` / `.env.local` は**読まない・書かない・コミットしない**。中身を画面に出さない
- APIキー・接続文字列・トークンの値を出力しない。必要なときはマスクする
- `SUPABASE_SERVICE_ROLE_KEY` に `NEXT_PUBLIC_` を付けない
- `users` テーブルに **update ポリシーを作らない**（自己昇格を防ぐため）
- seed の禁止タグに**実在の会社名・顧客名を入れない**（public になる）
- `service_role` を読むファイルの1行目に `import 'server-only'`
- LLM が返した `user_id` は**必ず候補リストと照合してから**使う
- 知見カードの `body` に発言を逐語引用しない。必ず要約する
- タグ付けエージェントAの入力は `is_agent = false` の発言だけ
- **話者を名前で照合しない**。文字起こしは `transcript_segments` に `user_id` 付きで入っている。
  LLMには行番号 `[s12]` だけを見せ、渡していない番号を返したら `ingest_status='needs_review'` で止める。
  エージェントは社員アカウントを作らない
- **タグを AI が `official` にしない**。候補・格上げ候補（`proposed`）までがAIの仕事。
  候補タグでも知見カードと `user_tags` は作る（状態で絞らない。正式化は管理者の承認だけ）
- 破壊的SQL（`drop` / `truncate` / `delete` / `db reset`）は**実行前に必ず人間に確認する**
- `git push` はしない。コミットまでで止める

---

## 設計を変えたくなったら

勝手に変えない。`docs/QUESTIONS.md` に以下の形式で書き、**先に進める部分を進める**。

```
## [日付] 見出し
- 困っていること:
- DESIGN.md のどこに関係するか:
- 候補A / 候補B:
- 暫定でどう進めたか:
```

---

## 作業の進め方

- 1タスク完了ごとに `git commit`（push はしない）
- コミットメッセージは日本語でよい。何をしたか1行
- 動作確認のログは `docs/evidence/` に残す（スクショの代わりになる）

---

## エージェント構成

| | 役割 | 起動 | 自律度 |
|---|---|---|---|
| 🎙️ A タグ付けエージェント | ライブの文字起こし → 知見カード＋タグ | ライブ終了 | Lv.2 チェーン |
| 🎪 B 場づくりエージェント | 場を立てるか判断 → 打診 → 日程調整 | 1日1回 自己起動 | **Lv.3 ループ** |
| 🔍 C 自己分析エージェント | 画面共有から本人の得意を拾う | ボタン | Lv.2 |

**口を持つのは B だけ。** 通知権限は B のキーにしか渡さない。

判断は3値: `open` / `wait` / `skip`
停止条件（コードで保証）: 14日 ／ 候補3人 ／ コスト上限
