-- =====================================================================
--  デモの管理者役を1人決める（人材育成部の松永 蒼）
--
--  IMPLEMENTATION-PLAN.md §4「ログイン」より:
--    「管理者役の架空社員を1人決める: 松永 蒼（人事部＝人材育成担当）を role='admin' にする」
--
--  ★ 対象は 0006_demo_seed.sql で投入した架空社員のみ。
--    実在の社員のroleには一切触れない（該当なしのはず）。
-- =====================================================================

begin;

update users
   set role = 'admin'
 where id = 'd7cac38d-592d-406a-bc61-2e195e31c6cf'   -- 松永 蒼
   and role <> 'admin';

commit;

-- =====================================================================
--  確認
--  期待 → 1件、display_name='松永 蒼', role='admin'
-- =====================================================================
select id, display_name, department, role
from users
where id = 'd7cac38d-592d-406a-bc61-2e195e31c6cf';
